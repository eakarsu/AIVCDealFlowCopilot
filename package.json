{
  "name": "ai-vc-deal-flow-copilot",
  "version": "1.0.0",
  "description": "AI-powered VC deal flow management — pipeline, IC memos, LP reporting, portfolio mgmt, follow-ons, exits",
  "private": true,
  "scripts": {
    "start": "bash start.sh",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "seed": "cd backend && node seed/seed.js",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  }
}
